# Context format

Record the domain purpose, actors, durable terms, invariants, boundaries, and links to qualifying ADRs. Each term has one definition and names common aliases only when they prevent ambiguity. Keep feature-specific implementation plans out of durable context.

