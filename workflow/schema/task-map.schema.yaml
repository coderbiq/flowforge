schema: flowforge/task-map@v2
description: Canonical bridge from proposal capabilities to task backend items.
type: object
required:
  - proposal_id
  - backend
  - tasks
properties:
  proposal_id:
    type: string
  backend:
    type: string
    enum:
      - beads
      - github
      - linear
      - none
  tasks:
    type: array
    items:
      type: object
      required:
        - task_id
        - title
        - outcome
        - capability_refs
        - completion_definition
      properties:
        task_id:
          type: string
        title:
          type: string
        outcome:
          type: string
        workspace:
          type: string
          description: Optional workspace name for the task when work spans multiple document spaces.
        code_scope:
          type: string
          description: Optional code scope or subdirectory the task targets.
        priority:
          type: string
          enum: [P0, P1, P2]
        depends_on:
          type: array
          items:
            type: string
        capability_refs:
          type: array
          items:
            type: string
        decision_refs:
          type: array
          items:
            type: string
        archive_target_refs:
          type: array
          items:
            type: string
        completion_definition:
          type: array
          items:
            type: string
